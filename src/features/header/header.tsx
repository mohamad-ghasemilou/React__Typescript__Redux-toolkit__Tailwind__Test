function Header() {
    return (
        <div className={styles.container}>
            header
        </div>
    );
}

export default Header;

const styles = {
    container: `h-20 bg-red-700 sticky top-0`
}
